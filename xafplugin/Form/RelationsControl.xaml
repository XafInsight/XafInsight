<UserControl x:Class="xafplugin.Form.RelationsControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:xafplugin.ViewModels"
        Height="500" Width="880"
        Background="#FFF">
    <UserControl.Resources>
        <Style x:Key="DeleteButtonStyle" TargetType="Button">
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="Width" Value="26"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFF"/>
            <Setter Property="BorderBrush" Value="#E85D5D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#E85D5D"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="ToolTip" Value="Delete relationship"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsMouseOver}" Value="True">
                    <Setter Property="Opacity" Value="1"/>
                </DataTrigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFE6E6"/>
                    <Setter Property="BorderBrush" Value="#D22"/>
                    <Setter Property="Foreground" Value="#D22"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="#F9F9F9"
                BorderBrush="#DDD"
                BorderThickness="1"
                Padding="20"
                Margin="0,0,16,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Relationships within the tables"
                           FontSize="17"
                           FontWeight="Bold"
                           Margin="0,0,0,12"/>

                <TextBlock Grid.Row="1"
                           Text="No relationships found"
                           Foreground="#BBB"
                           FontStyle="Italic"
                           FontSize="15"
                           HorizontalAlignment="Left"
                           Margin="0,0,0,12">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Relations.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <ListBox x:Name="ListRelations"
                         Grid.Row="2"
                         ItemsSource="{Binding Relations}" 
                         BorderThickness="0"
                         Background="Transparent"
                         VerticalAlignment="Stretch"
                         HorizontalAlignment="Stretch">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0" VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding MainTable}" FontWeight="Bold" />
                                    <TextBlock Text=" (" />
                                    <TextBlock Text="{Binding MainTableColumn}" />
                                    <TextBlock Text=") -> " Margin="5,0"/>
                                    <TextBlock Text="{Binding RelatedTable}" />
                                    <TextBlock Text=" (" />
                                    <TextBlock Text="{Binding RelatedTableColumn}" />
                                    <TextBlock Text=")" />
                                    <TextBlock Text=" (" />
                                    <TextBlock Text="{Binding JoinType}" />
                                    <TextBlock Text=")" />
                                </StackPanel>
                                <Button Grid.Column="1"
                                        Style="{StaticResource DeleteButtonStyle}"
                                        Click="BtnVerwijder_Click"
                                        Tag="{Binding}">
                                    <TextBlock Text="🗑" FontSize="14" />
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Border Grid.Column="1"
                Background="#FFF"
                BorderBrush="#DDD"
                BorderThickness="1"
                Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock Text="Add/Edit relationship" FontSize="16" FontWeight="Bold" Margin="0,0,0,16"/>

                    <TextBlock Text="Main table" Margin="0,0,0,4"/>
                    <ComboBox x:Name="CbMainTable" MinWidth="150" Margin="0,0,0,10"
                              ItemsSource="{Binding Tables}"
                              SelectedItem="{Binding MainTable, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Main column" Margin="0,0,0,4"/>
                    <ComboBox x:Name="CbMainColumn" MinWidth="150" Margin="0,0,0,10"
                              ItemsSource="{Binding MainTableColumns}"
                              SelectedItem="{Binding MainTableColumn, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Child table" Margin="0,0,0,4"/>
                    <ComboBox x:Name="CbRelatedTable" MinWidth="150" Margin="0,0,0,10"
                              ItemsSource="{Binding Tables}"
                              SelectedItem="{Binding RelatedTable, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Child column" Margin="0,0,0,4"/>
                    <ComboBox x:Name="CbRelatedColumn" MinWidth="150" Margin="0,0,0,10"
                              ItemsSource="{Binding RelatedTableColumns}"
                              SelectedItem="{Binding RelatedTableColumn, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Join type" Margin="0,0,0,4"/>
                    <ComboBox x:Name="CbJoinType" MinWidth="150" Margin="0,0,0,20"
                              ItemsSource="{Binding JoinTypes}"
                              SelectedItem="{Binding JoinType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Save" Width="80" Margin="0,0,8,0" Click="BtnToevoegen_Click"/>
                    <Button Content="Cancel" Width="80" Click="BtnAnnuleren_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
